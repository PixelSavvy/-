export interface navigationLinksTypes {
  label: string;
  to: string;
  collapsed: boolean;
}

export interface sideMenuNavigationLinksTypes {
  serviceTitle: string;
  serviceId: string;
  collapsed: boolean;
}
